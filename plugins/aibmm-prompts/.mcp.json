{
  "mcpServers": {
    "aibmm": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "AIBMM_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
